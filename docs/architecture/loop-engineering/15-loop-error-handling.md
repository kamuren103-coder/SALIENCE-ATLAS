# Loop Error Handling (Salience Atlas v5)

## Overview
Declares a rigid class hierarchy of loop-centric exceptions (e.g. `PlanningError`, `ExecutionError`, `ValidationError`, `TimeoutError`, `CancellationError`). All exceptions support inner cause nested tracing.
