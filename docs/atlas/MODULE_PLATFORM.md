# Atlas Module Platform

`packages/platform` provides the shared `AtlasModuleDefinition` contract and registry. Definitions can declare ontology entities, views, metrics, intelligence capabilities, workflows, actions, events, and permissions. Existing modules remain registered and functional; `atlas-demo` demonstrates configuration-driven reuse without replacing Inventory, Logistics, or Command Center.
