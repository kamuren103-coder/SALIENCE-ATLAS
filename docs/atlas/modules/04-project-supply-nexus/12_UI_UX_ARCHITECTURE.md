# UI/UX Architecture

The existing Nexus surface now prioritizes truth states: requirement evidence,
live warning count and connection status. Hardcoded BOM percentages,
line-item counts and readiness bars were removed from the primary card.
Progressive disclosure should next add requirement gaps, shipment evidence and
milestone exposure only after those sources are persisted and authorized.
