# AI Intelligence Model

No isolated supplier AI stack is introduced. Future Supplier Risk,
Performance, Relationship and Dependency Analysts must use governed tools,
tenant/resource authorization and evidence classifications (`FACT`, `DERIVED`,
`INFERENCE`, `UNKNOWN`, `UNAVAILABLE`). Phase 03 calculations are
deterministic, not AI predictions.
