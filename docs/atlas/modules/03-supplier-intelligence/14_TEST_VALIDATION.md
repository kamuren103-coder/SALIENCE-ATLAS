# Test and Validation

Validation covers deterministic supplier performance calculations, API/router
type checking, tenant authorization shape, graph scoping, audit invocation
and production build. Runtime API validation requires authenticated tenant
context and configured database.
