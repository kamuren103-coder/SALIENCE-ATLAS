# Graph Model

The intended chain is `SHIPMENT -> CARGO -> MATERIAL -> WAREHOUSE -> PROJECT ->
CONTRACT -> SUPPLIER -> ASSET -> GRID_IMPACT`. Existing logistics graph and
relationship registries remain the shared integration point. Phase 02 does not
create a second graph or assert relationships without persisted evidence.
