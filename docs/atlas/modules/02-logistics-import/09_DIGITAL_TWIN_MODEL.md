# Digital Twin Model

The existing twin route derives a logistics entity view from persisted
facility, stock, shipment and event records. A twin may expose identity,
location, state, movement history and source freshness. It must not display
synthetic vessel location, customs state or predicted ETA as fact.
